package com.samduka.dukacred.core.designsystem

import androidx.compose.ui.graphics.Color

object DukaCredColors {

    // ── Brand ─────────────────────────────────────────────────────────
    val ForestGreen900  = Color(0xFF0D2B1F)   // deepest — backgrounds
    val ForestGreen800  = Color(0xFF133527)   // surface
    val ForestGreen700  = Color(0xFF1A4A35)   // elevated surface
    val ForestGreen600  = Color(0xFF1F5C41)   // card surface
    val ForestGreen500  = Color(0xFF257A56)   // primary brand
    val ForestGreen400  = Color(0xFF2E9E6E)   // primary lighter
    val ForestGreen300  = Color(0xFF52B88A)   // secondary accent

    // ── Ochre / Amber ─────────────────────────────────────────────────
    val Ochre500        = Color(0xFFD4860A)   // merchant identity
    val Ochre400        = Color(0xFFE8990F)   // merchant lighter
    val Ochre300        = Color(0xFFF5B830)   // merchant highlight
    val Ochre100        = Color(0xFFFDF3DC)   // ochre tint

    // ── Cream / Warm White ────────────────────────────────────────────
    val Cream100        = Color(0xFFFBF8F2)   // text on dark
    val Cream200        = Color(0xFFF2EDE0)   // secondary text
    val Cream300        = Color(0xFFE0D8C8)   // tertiary text / borders

    // ── Neutrals ──────────────────────────────────────────────────────
    val Charcoal900     = Color(0xFF0F1410)
    val Charcoal800     = Color(0xFF1C2420)
    val Charcoal700     = Color(0xFF2A352E)

    // ── Semantic ──────────────────────────────────────────────────────
    val Success         = Color(0xFF2E9E6E)
    val Warning         = Color(0xFFE8990F)
    val Error           = Color(0xFFD94F4F)
    val ErrorLight      = Color(0xFFFDECEC)

    // ── Overlays ──────────────────────────────────────────────────────
    val BlackAlpha40    = Color(0x66000000)
    val BlackAlpha60    = Color(0x99000000)
    val WhiteAlpha10    = Color(0x1AFFFFFF)
    val WhiteAlpha20    = Color(0x33FFFFFF)
}